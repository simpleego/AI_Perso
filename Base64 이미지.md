## 🖼️ Base64 인코딩 이미지란?

**Base64 인코딩 이미지**란 바이너리(이진수) 데이터로 이루어진 이미지 파일(JPG, PNG, GIF 등)을 **텍스트(ASCII 문자) 형태로 변환**한 것을 말합니다. 

일반적으로 HTML이나 CSS에서 이미지를 불러올 때는 `<img src="image.png">`처럼 파일 경로를 지정하지만, Base64로 변환하면 **이미지 데이터 자체를 텍스트 문자열로 직접 코드 안에 심어 넣을 수 있습니다.**

**[코드 예시]**
```html
<!-- 일반 이미지 태그 -->
<img src="logo.png" alt="로고">

<!-- Base64 인코딩 이미지 태그 (Data URI Scheme) -->
<img src="data:image/png;base64,iVBORw0KGgoAAAANSUhEUgAA..." alt="로고">
```
*(위에서 `iVBORw0KGgo...`로 시작하는 긴 문자열이 바로 이미지 파일이 텍스트로 변환된 형태입니다.)*

---

## ✅ 언제 사용해야 하는가? (장점 및 활용 사례)

Base64 인코딩은 모든 상황에 적합한 것은 아니지만, **특정 상황에서는 매우 강력한 장점**을 발휘합니다.

### 1. HTTP 요청 수를 줄이고 싶을 때 (아주 작은 이미지)
웹페이지가 로딩될 때 브라우저는 이미지 파일을 가져오기 위해 서버에 HTTP 요청을 보냅니다. 요청이 많아지면 로딩 속도가 느려집니다. 
* **활용:** **로딩 스피너, 아주 작은 아이콘, 1x1 픽셀 투명 이미지, 배경 패턴** 등 크기가 작은 이미지는 Base64로 변환해 CSS나 HTML에 직접 포함시키면 서버 요청 없이 즉시 표시할 수 있어 로딩 속도를 개선할 수 있습니다.

### 2. 파일을 하나로 묶어 배포하거나 공유할 때
이미지 파일이 별도로 존재하면 링크가 깨지거나 파일이 누락될 수 있습니다.
* **활용:** HTML, CSS, JSON, XML 파일 하나에 이미지를 포함시켜 **단일 파일로 배포**해야 할 때 유용합니다. (예: 오프라인에서 열리는 프레젠테이션용 HTML, 이메일 뉴스레터 템플릿)

### 3. API(JSON)를 통해 이미지를 전송할 때
REST API를 통해 서버와 클라이언트가 데이터를 주고받을 때 주로 JSON 형식을 사용합니다. JSON은 텍스트 기반이므로 바이너리 이미지를 직접 넣을 수 없습니다.
* **활용:** 프로필 사진 업로드/다운로드 API나 채팅방 이미지 전송 시 이미지를 Base64 문자열로 변환하여 JSON 페이로드에 포함해 전송합니다.

### 4. 보안이 필요한 내부망 환경
외부 이미지 URL을 참조할 수 없는 폐쇄적인 네트워크 환경이나, 보안을 위해 외부 도메인 참조를 차단한 경우 유용합니다.

---

## ❌ 언제 사용하면 안 되는가? (단점 및 주의사항)

Base64 인코딩은 만능이 아니며, **잘못 사용하면 오히려 성능을 저하**시킵니다.

### 1. 파일 크기가 약 33% 증가함
Base64는 바이너리를 텍스트로 변환하는 과정에서 **원본 파일보다 데이터 크기가 약 1.33배(33%) 커집니다.** 따라서 큰 이미지를 Base64로 변환하면 페이지 용량이 불필요하게 늘어납니다.

### 2. 브라우저 캐싱(Caching)이 불가능함
일반 이미지는 브라우저가 한 번 다운로드하면 캐시에 저장해 두고 다음에 재사용합니다. 하지만 CSS나 HTML 코드 안에 Base64로 박혀 있는 이미지는 **웹페이지(또는 CSS 파일) 전체를 다시 다운로드해야만** 이미지를 갱신할 수 있습니다.

### 3. 코드 가독성 및 유지보수 저하
소스 코드 안에 수천 줄의 Base64 문자열이 들어가면 코드를 읽거나 수정하기가 매우 어려워집니다.

---

## 💡 요약 및 실무 가이드라인

| 구분 | 일반 이미지 (`src="url"`) | Base64 인코딩 이미지 |
| :--- | :--- | :--- |
| **서버 요청** | 발생함 (요청 수 증가) | 발생하지 않음 (즉시 로딩) |
| **파일 크기** | 원본 그대로 | 원본보다 **약 33% 증가** |
| **캐싱** | 브라우저가 별도 캐싱 가능 | HTML/CSS와 함께 캐싱됨 |
| **유지보수** | 파일만 교체하면 됨 | 코드 내부의 문자열을 수정해야 함 |

**🎯 실무 추천 기준:**
* **사용 추천:** **10KB 이하**의 아주 작은 아이콘, UI 요소, 단일 파일 배포가 필요한 경우, JSON API 전송.
* **사용 금지:** **10KB 이상**의 일반적인 사진, 배너, 로고 등 자주 바뀌거나 별도로 캐싱되어야 하는 이미지.
