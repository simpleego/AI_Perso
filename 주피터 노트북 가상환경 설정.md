아나콘다(Anaconda) 가상환경을 주피터 노트북에 연결하면, 각 프로젝트마다 필요한 라이브러리 버전을 독립적으로 관리할 수 있어 매우 효율적입니다.

핵심은 가상환경에 커널(Kernel)을 연결해주는 `ipykernel` 패키지를 설치하는 것입니다. 아래 단축 가이드를 따라 진행해 보세요.

---

## 1. 가상환경 생성 및 활성화

먼저 연결할 가상환경이 준비되어 있어야 합니다. (이미 있다면 활성화 단계로 넘어가세요.)

1. **Anaconda Prompt** (또는 터미널)를 실행합니다.
2. 가상환경을 생성합니다. (예: 이름은 `myenv`, 파이썬 3.10 버전)
```bash
conda create -n myenv python=3.10

```


3. 생성한 가상환경을 활성화합니다.
```bash
conda activate myenv

```



---

## 2. 필수 패키지 설치

주피터 노트북이 가상환경을 인식할 수 있도록 도구를 설치해야 합니다. 활성화된 상태에서 아래 명령어를 입력하세요.

```bash
conda install ipykernel

```

또는 `pip`를 사용해도 무방합니다.

```bash
pip install ipykernel

```

---

## 3. 주피터 노트북에 가상환경 등록

이제 현재 가상환경을 주피터 노트북의 '커널'로 등록하는 마지막 단계입니다.

```bash
python -m ipykernel install --user --name myenv --display-name "Python (MyProject)"

```

* `--name myenv`: 시스템이 인식할 가상환경의 실제 이름입니다.
* `--display-name "Python (MyProject)"`: 주피터 노트북 화면상에 표시될 이름입니다. (자유롭게 설정 가능)

---

## 4. 주피터 노트북에서 확인 및 변경

설정을 마쳤다면 주피터 노트북을 실행합니다.

1. **Jupyter Notebook 실행:** `jupyter notebook` 입력
2. **커널 선택:**
* 새 노트를 만들 때: 오른쪽 상단 **New** 버튼을 누르면 방금 등록한 `Python (MyProject)`가 리스트에 보입니다.
* 이미 열린 노트에서 바꿀 때: 상단 메뉴의 **Kernel** → **Change kernel** → 등록한 가상환경 선택



---

### 💡 팁: 가상환경 연결 해제하기 (삭제)

만약 더 이상 해당 가상환경을 주피터 리스트에서 보고 싶지 않다면, 아래 명령어로 등록된 커널만 삭제할 수 있습니다.

```bash
jupyter kernelspec uninstall myenv

```

이 과정을 통해 각 프로젝트 성격에 맞는 독립적인 개발 환경을 주피터 노트북에서 간편하게 전환하며 사용하실 수 있습니다. 가상환경 연결 중 오류가 발생하면 말씀해 주세요!

```

```
