import numpy as np
import sounddevice as sd

# 1. 기본 설정
sr = 16000      # 샘플링 레이트 (1초에 16000개 샘플)
duration = 0.5  # 각 음의 길이 (0.5초)
t = np.linspace(0, duration, int(sr * duration), endpoint=False) # 시간축 생성

# 2. 음계별 주파수 정의 (4옥타브 기준 Hz)
notes = {
    '도': 261.63,
    '레': 293.66,
    '미': 329.63,
    '파': 349.23,
    '솔': 392.00,
    '라': 440.00,
    '시': 493.88,
    '높은도': 523.25
}

def generate_tone(frequency, sr, t):
    """특정 주파수의 사인파를 생성하고 소리를 부드럽게 만들기 위해 페이드 인/아웃 적용"""
    # 기본 사인파 생성
    wave = np.sin(2 * np.pi * frequency * t)
    
    # 뚝 끊기는 소리(클릭 노이즈)를 방지하기 위한 간단한 외포선(Envelope) 적용
    envelope = np.ones_like(t)
    fade_len = int(sr * 0.05) # 0.05초 동안 페이드
    envelope[:fade_len] = np.linspace(0, 1, fade_len)
    envelope[-fade_len:] = np.linspace(1, 0, fade_len)
    
    return wave * envelope

# 3. 사용자 입력 및 출력 부분
print("--- 간단한 파이썬 피아노 ---")
print("입력 가능: 도, 레, 미, 파, 솔, 라, 시, 높은도 (종료: q)")

while True:
    user_input = input("\n연주할 음을 입력하세요: ").strip()
    
    if user_input.lower() == 'q':
        print("프로그램을 종료합니다.")
        break
        
    if user_input in notes:
        freq = notes[user_input]
        wave = generate_tone(freq, sr, t)
        
        print(f"🎵 '{user_input}' ({freq} Hz) 재생 중...")
        sd.play(wave, sr)
        sd.wait()  # 재생이 끝날 때까지 대기
    else:
        print("❌ '도, 레, 미...' 형식으로 정확히 입력해주세요.")

---


```python
import numpy as np
from IPython.display import Audio, display

# 1. 기본 설정
sr = 16000      # 샘플링 레이트
duration = 0.5  # 각 음의 길이 (초)
t = np.linspace(0, duration, int(sr * duration), endpoint=False)

# 2. 음계별 주파수 정의 (4옥타브 기준)
notes = {
    '도': 261.63,
    '레': 293.66,
    '미': 329.63,
    '파': 349.23,
    '솔': 392.00,
    '라': 440.00,
    '시': 493.88,
    '높은도': 523.25
}

def generate_tone(frequency, sr, t):
    """사인파 생성 및 페이드 인/아웃 적용"""
    wave = np.sin(2 * np.pi * frequency * t)

    # 클릭 노이즈 방지용 엔벨로프
    envelope = np.ones_like(t)
    fade_len = int(sr * 0.05)
    envelope[:fade_len] = np.linspace(0, 1, fade_len)
    envelope[-fade_len:] = np.linspace(1, 0, fade_len)

    return wave * envelope

# 3. 사용자 입력 및 출력 부분
print("--- Colab 파이썬 피아노 ---")
print("입력 가능: 도, 레, 미, 파, 솔, 라, 시, 높은도 (종료: q)")

while True:
    user_input = input("\n연주할 음을 입력하세요: ").strip()

    if user_input.lower() == 'q':
        print("프로그램을 종료합니다.")
        break

    if user_input in notes:
        freq = notes[user_input]
        wave = generate_tone(freq, sr, t)

        print(f"🎵 '{user_input}' ({freq} Hz) 재생 컨트롤러 생성")
        # Colab 브라우저에서 소리를 재생할 수 있는 오디오 객체 생성 및 표시
        display(Audio(wave, rate=sr, autoplay=True))
    else:
        print("❌ '도, 레, 미...' 형식으로 정확히 입력해주세요.")
```        
